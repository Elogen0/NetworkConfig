# 버추얼박스 설치
VM 폴더 : D:\VirtuslBox  만들어서 선택  
- 설정사항 : 일반 : Features-양방향, 양방향
  - 시스템 : cpu :2     (쿠버네티스 마스터는 cpu 2개 이상 필요)
  - memory: 2G
  - **UEFI 체크**
  - **마더보드 - Pointing Device - ps/2 마우스 선택**
  - 디스크 : vm1,2...용량 25G (마스터는 50G)
  - 네트워크 : 어댑터 브리지, Promiscuous Mode : 모두 허용
  - 디스플레이 : 3D 가속 체크
  - 공유폴더 : 추가버튼  D:/vm_share
  - Mount Point : /vm_share,  읽기 전용 빼고 다 체크  

# 우분투 설치
## 실습을 위한 기초적인 프로그램 인스톨
```bash
sudo apt update
sudo apt install -y build-essential dkms linux-headers-$(uname -r)  

sudo apt install -y openssh-server tree net-tools ufw nmap git curl wget 
sudo apt install -y neofetch  htop
sudo apt install -y numlockx sshpass 
sudo apt install -y ca-certificates  gnupg lsb-release  apt-transport-https 

sudo apt install  -y software-properties-common   
sudo add-apt-repository --yes --update ppa:ansible/ansible  
sudo apt install ansible -y
```

## Swap 및 방화벽 설정
쿠버네티스서 swap이 켜져있으면 정확한 메모리 리소스를 유추할 수 없기에 꺼야합니다.
### 수동으로 설정하는 방법
설치후 스토리지 파티션 설정 : $ sudo swapoff -a     
     부팅시 자동 마운 해제 : $ sudo vi /etc/fstab    

파일에서 맨 앞에 주석처리(#추가)
```
# /swap.img none  swap sw 0  0
```

### Regex를 이용하여 설정하는 방법
```bash
sudo swapoff -a
sudo sed -i '/ swap / s/^\(.*\)$/#\1/g' /etc/fstab
# 실습 편의를 위해 방화벽 비활성화 또는 필수 포트(UDP 8472, TCP 179 등) 개방

```

## 커널 모듈 및 브릿지 활성화
```bash
cat <<EOF | sudo tee /etc/sysctl.d/k8s.conf
net.bridge.bridge-nf-call-iptables  = 1
net.bridge.bridge-nf-call-ip6tables = 1
net.ipv4.ip_forward                 = 1
EOF
sudo sysctl --system

```

## sudo 명령어 안쳐도 되는 설정
`sudo visudo`
쳐서 나오는 파일 맨 마지막 줄에 추가 ($USER_NAME은 현재 유저이름으로 바꿔서)
```
$USER_NAME ALL=(ALL) NOPASSWD: ALL
```
